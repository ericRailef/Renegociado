/**
 * Representa la informacion de salida (considerando solo el contenido dentro de Data)
 * por el servicio LIDO.
 */
export class LIDORequest {
  RutUsuario: string;
  canalLogico: string;
  usuarioAltair: string;
  idSolicitud: string;
  puntoImpresion: string;
}
