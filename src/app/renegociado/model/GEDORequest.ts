/**
 * Representa la informacion de salida (considerando solo el contenido dentro de Data)
 * por el servicio GEDO.
 */
export class GEDORequest {
  RutUsuario: string;
  tipoDocumental: string;
  piolDocumento: string;
}
